import 'package:flutter/material.dart';

class SearchTaskView extends StatelessWidget {
  const SearchTaskView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Search Tasks View'),
    );
  }
}
